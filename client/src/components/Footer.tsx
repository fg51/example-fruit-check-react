import React from 'react';
import { IonFooter } from '@ionic/react';

const Footer: React.FC = () => {
  return (
    <IonFooter>
      作成日: 2020-05-27 作成者: Alice 承認者: Bob
   </IonFooter>
  );
};

export default Footer;
